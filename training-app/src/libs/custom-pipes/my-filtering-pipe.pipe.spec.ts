import { MyFilteringPipePipe } from './my-filtering-pipe.pipe';

describe('MyFilteringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyFilteringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
